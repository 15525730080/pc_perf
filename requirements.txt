# ── Web 框架 ──────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── 数据库 ────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0    # ORM + async engine
aiosqlite>=0.20.0             # SQLite async 驱动（SQLAlchemy 后端）

# ── iOS 采集 ──────────────────────────────────────────────────
py-ios-device>=2.0.0          # Instruments DTX 协议（sysmontap / graphics）
# go-ios 二进制需单独安装，见 README

# ── Android 采集 ──────────────────────────────────────────────
adbutils>=2.8.0

# ── PC 采集 ───────────────────────────────────────────────────
psutil>=5.9.0
pynvml>=11.5.0                # NVIDIA GPU（可选）

# ── 报表 ──────────────────────────────────────────────────────
openpyxl>=3.1.0

# ── 调度 ──────────────────────────────────────────────────────
apscheduler>=3.10.0

# ── 工具 ──────────────────────────────────────────────────────
Pillow>=10.0.0                # PC 截图（可选）
pygetwindow>=0.0.9            # Windows 窗口管理（截图用）
